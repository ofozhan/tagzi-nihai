import { StyleSheet } from 'react-native';

export const lightColors = { /* ... önceki mesajdaki gibi ... */ };
export const darkColors = { /* ... önceki mesajdaki gibi ... */ };
export const getStyles = (colors) => StyleSheet.create({ /* ... önceki mesajdaki gibi ... */ });